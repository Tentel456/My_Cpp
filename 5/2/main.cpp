#include <iostream>

int main()
{
    //Здесь инициализация переменных будет прямой, без brace, только для понимания литералов
    int x = 15; //Это литерал, поскольку указывается напрямую в коде
    int y = 300; //Это тоже литерал
    int sum = x + y;
    std::cout << sum;
    return 0;
}
